export interface userData {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
    avatarURL: string;
    }

export interface loginData {
    email: string;
    password: string;
}

export interface detinationData {
    name: string;
    location: string;
    description: string;
    image: string;
    }